# Docker Compose Environment Variables
# Copy this file to .env and update with your production values

# ==============================================
# SECURITY (REQUIRED - MUST CHANGE IN PRODUCTION)
# ==============================================

# JWT Secret - MUST be at least 32 characters, cryptographically secure
# Generate with: openssl rand -base64 32
JWT_SECRET=CHANGE_THIS_TO_A_SECURE_32_PLUS_CHARACTER_SECRET

# Session Secret - MUST be cryptographically secure
# Generate with: openssl rand -base64 32
SESSION_SECRET=CHANGE_THIS_TO_A_SECURE_SESSION_SECRET

# Redis Password - Used for Redis authentication
# Generate with: openssl rand -base64 24
REDIS_PASSWORD=changeme

# MongoDB Password (if using MongoDB authentication)
# Generate with: openssl rand -base64 24
# MONGO_PASSWORD=changeme

# ==============================================
# APPLICATION URLs
# ==============================================

# Frontend URL - Update with your production domain
FRONTEND_URL=http://localhost:3000

# Backend URL - Update with your production domain
BACKEND_URL=http://localhost:5001

# React App API URL - Used by frontend
REACT_APP_API_URL=http://localhost:5001/api

# ==============================================
# OAUTH (OPTIONAL)
# ==============================================

# Google OAuth credentials (optional)
# Get from: https://console.cloud.google.com/apis/credentials
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
GOOGLE_CALLBACK_URL=http://localhost:5001/api/auth/google/callback

# ==============================================
# NOTES
# ==============================================

# 1. Never commit .env file to version control
# 2. All secrets should be generated using cryptographically secure methods
# 3. In production, use environment variable injection from your hosting platform
# 4. For MongoDB authentication, uncomment MONGO_INITDB_ROOT_USERNAME and 
#    MONGO_INITDB_ROOT_PASSWORD in docker-compose.yml and update MONGODB_URI
